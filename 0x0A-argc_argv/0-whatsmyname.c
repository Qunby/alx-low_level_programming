#include <stdio.h>

/**
 * main - prints its name followed by a new line
 * @argc: the argument count
 * @argv: the argument to be passed
 * Return: Always o
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
